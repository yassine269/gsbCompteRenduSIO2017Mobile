<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="bts.sio.compterendu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PanelVisiteurActivity"></activity>
        <activity android:name=".ConsulterCRActivity"></activity>
        <activity android:name=".PanelDelegueActivity"></activity>
        <activity android:name=".PanelResponsableActivity"></activity>
        <activity android:name=".SaisieCRActivity"></activity>
        <activity android:name=".AndroidSpinnerExampleActivity"></activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET"/>

</manifest>